<resources>

    <!-- TOOLBAR -->
    <style name="MyToolbar" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/MyToolbar.TitleText</item>
        <item name="titleTextStyle">@style/MyToolbar.TitleText</item>

        <item name="android:subtitleTextStyle">@style/MyToolbar.SubtitleText</item>
        <item name="subtitleTextStyle">@style/MyToolbar.SubtitleText</item>
    </style>

    <style name="MyToolbar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyToolbar.SubtitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Subtitle">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
